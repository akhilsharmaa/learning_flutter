import 'package:flutter/material.dart';

class Contry extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("India"),backgroundColor: Colors.pinkAccent,
      ),
    );
  }
}
